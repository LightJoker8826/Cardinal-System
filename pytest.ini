[pytest]
markers =
    chaos: tests that assert intentional bug triggers exist (excluded from the healer's post-patch verification run)
    agent: tests that exercise the healing/gate pipeline itself (excluded from the healer's post-patch verification run to prevent recursion)
testpaths = tests
